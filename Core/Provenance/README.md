# Provenance

Provenance хранит происхождение производных результатов.

Минимальная цепочка:

```text
result → algorithm/model → input observations → source → timestamp
```

Без этой цепочки результат не должен считаться полноценно аудитируемым.
